export type Pokemon = {
    name: string;
    id: string;
    imgSrc: string;
}

export type PokemonDetails = {
    name: string;
    id: string;
    imgSrc: string;
    hp: string;
    attack: string;
    defense: string;
}